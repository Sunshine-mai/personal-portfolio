feat: 技术网络图加 3D 纵深感，并修正漂移方向（节点不再逃离光标）

一、修正一个真实缺陷：漂移方向搞反了

所有者反馈「悬停到具体的图标不要移动啊，我想点击它它一直跑」。

根因不是参数没调好，而是方向错了：第一版把漂移做成**径向**——把节点推离光标，
于是节点逃得越来越远，根本点不到。

改为**切向**（绕光标流动）：位移不改变节点到光标的距离，既有流动感又点得到。
并加断言守住：悬停节点的内层 translate 必须为 0（实测 0）。
没有这条断言，很容易又被改回径向。

二、3D 纵深感（所有者要求"漂浮、跟随鼠标"）

- 整张图随鼠标倾斜（±7°/±5° 透视旋转），容器级 transform，不影响节点基准坐标
- 每个节点有一个由 id 哈希派生的**固定深度**，决定点径（0.78~1.22 倍）与填充透明度
  用哈希而非随机：深度每次构建必须相同，否则依赖几何的断言不再可靠
- 靠近光标的节点略微变大（最多 1.38 倍），强化"跟随鼠标"的感觉
- 悬停节点仍保持放大 1.5 倍且**完全不位移**

三、分层结构仍然成立（这是能同时"动"与"可测"的原因）

外层 <g> 基准坐标任何时候不改（断言读它），动画与倾斜只写内层与容器。
本轮新增两条断言：
- 悬停的节点不会被推开（点得到）→ 实测 0
- 容器随鼠标倾斜（3D 感）→ 读的是容器实际计算样式，不是"有没有写这段代码"

prefers-reduced-motion 下倾斜与漂移全部取消。

验收：61 → 63 项，全部通过。
