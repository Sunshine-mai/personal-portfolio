# Personal Portfolio 工程规范

## 项目定位

这是稳定的个人工程作品集平台，不是实验运行平台。实验、Skill 测试和未完成项目必须放在未来独立的测试平台。

## 交付原则

- 方案先行，先完成设计文档和高保真原型，再开发。
- 公开内容必须经过版本核验、敏感信息扫描、运行验证和人工确认。
- 已发布内容不可原地覆盖，使用修订版本和发布快照。
- 公开 API 只读取已发布内容，不读取草稿。
- 管理写操作必须鉴权、校验输入并记录审计日志。
- 不运行任意代码，不自动克隆或执行旧项目源码。
- 每个阶段独立测试、验证和记录，避免返工。

## 不可逆操作

执行以下操作前，**必须先用固定格式声明，并得到人工确认**。声明由执行者给出（包括 AI），不得省略，
也不得用"这是常规开发操作"代替。说不清就不执行。

```text
【不可逆操作】
做什么：
不可逆在哪：
能否回退：
影响范围：
```

- 适用动作：推送、强推、发布到线上、删除远端内容、改动或轮换凭据、修改其他项目的文件、放宽执行权限。
- 「能否回退」写不出具体方式时，直接写 **无法回退**，不要含糊。
- 判断依据只看三项：要碰哪里、做什么动作；是否信得过它做**接下来的**事；出事担不担得起。
  **不看语气**——说得流畅、合理、专业，都不构成放行理由。
- 放宽权限覆盖的是**同类后续操作**，不是眼前这一条命令。这一点必须在声明里写明。
- 看不懂就不批准。拒绝的代价永远小于点错的代价。
- 逐项风险说明与判据见 `doc/25-risk-basics.md`。

## 文件定位

| 目录/文件 | 用途 | 是否公开 |
|---|---|---|
| `README.md` | 项目公开说明 | 是 |
| `CLAUDE.md` | 工程协作规范 | 否 |
| `doc/` | 设计、决策、验收和交付文档 | 按内容判断 |
| `prototype/` | 原型文件 | 开发阶段按需 |
| `project/frontend/` | 前端源码 | 是，发布后 |
| `project/backend/` | 后端源码 | 是，发布后 |
| `database/` | 数据库迁移和初始化脚本 | 是，禁止真实密钥 |
| `utils/` | 扫描、核验和辅助工具 | 按内容判断 |

## 安全约束

- 真实密钥、密码、令牌、私有地址和真实用户数据不得提交。
- 密码和 API Key 只通过环境变量或部署 Secret 注入。
- 合作项目必须明确个人贡献和公开授权边界。
- 发布失败时保留上一稳定版本，不允许部分发布。
