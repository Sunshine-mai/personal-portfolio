feat: 首页技术网络图，并把详情页截图移到技术栈之前

一、首页技术网络图（路线阶段 1）
- 新增 project/frontend/src/data/techGraph.js：纯函数，从各案例的 stack 派生节点与边
- 新增 project/frontend/src/components/TechGraph.vue：SVG + 真实 DOM 节点
- 首页新增 #stack 区块（放在"代表项目"之后），导航加"技术栈"标签

用二部图（项目 ↔ 技术），不画"技术↔技术"的线：
后者会给每个项目生成一个完全图，5 个项目约 225 条边、密度约 29%，必然成为毛线球。
实际结果 5 + 36 个节点、67 条边、密度约 8%（与设计稿预估一致）。
而且二部图的每条边都是可核验的事实（"Folio 用了 MySQL"），不是推断。

技术选择：SVG + DOM 而非 canvas（canvas 对屏幕阅读器完全不可见）；
固定种子的确定性布局（实时力导向每次位置都不同，验收断言不了）；不引入任何依赖；
提供与图等价的文字版清单（图挂了不丢信息，也防止两处数据漂移）。

二、把"挤不挤"变成可测量的判据（这一步抓到了真问题）
- 先写的判据是节点最小间距，断言立刻失败：实际最小间距只有 16px，标签必然互相压住
- 但节点间距本身是错的判据：两个节点横向相距 40px、纵向同高时，
  节点间距看着够，标签却必然重叠
- 改为**标签框重叠量**判据后重写布局：按标签框分离而不是按点距分离，
  画布放大到 1000×680。现在重叠量为 0，是数学保证而不是观感判断
- 这一轮图像识别工具两次返回错误，反而促使我把观感问题变成了可计算的判据

三、详情页 IA 顺序修正
截图（结果）移到技术栈（解释）之前——原来的顺序要求读者先消化一堆技术名词才看到产品。
并加断言用 DOM 顺序把它固定住：截图区必须在技术栈之前。

验收：47 → 59 项，全部通过。
