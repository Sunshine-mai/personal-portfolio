feat: 技术网络图加动画——悬停放大、鼠标附近漂移

按项目所有者反馈"想让它动起来"实现：
- 悬停节点放大到 1.5 倍，相邻节点 1.14 倍
- 鼠标半径 210 单位内的技术节点被轻轻推开（最多 20 单位，越近推得越多）；
  项目节点钉住不动——它们是图的骨架，动了反而看不清结构

关键结构：把动画与定位分层
- 外层 <g> 的基准坐标任何时候都不改
- 动画只写内层 <g> 的 transform

这不是为了整洁，而是解决一个真实矛盾：
原先决定"不做持续动画"的理由是"实时物理图每次位置都不同，验收断言不了"。
所有者希望动起来，但那个顾虑仍然成立。分层之后两边都成立——
位置断言读外层基准坐标（确定、可截图比对），动画写内层（不参与定位断言）。

动画由一次性 requestAnimationFrame 缓动循环驱动；指针离开并稳定后写入空 transform
并停止循环，静态时不占用 CPU。prefers-reduced-motion 时完全不做动画。

新增三条可测量断言：
- 悬停会放大节点（实测 1.49，目标 1.5）
- 静止后动画归零（所有内层 transform 为空）——证明循环确实停了，不空转
- 标签互不重叠与节点在画布内都读基准坐标，不受动画干扰

设计稿 doc/26 记录本次变更：原决定的顾虑与"动起来"的诉求如何同时满足。

验收：59 → 61 项，全部通过。
