# 测试与验收方案

## 1. 测试层级

```text
静态检查/内容规则
→ 单元测试
→ API 与数据库集成测试
→ 浏览器端到端测试
→ 安全/依赖/秘密扫描
→ 候选部署冒烟
→ 生产发布后验证
```

## 2. 核心单元测试

- 发布状态机所有分支。
- 项目字段和 URL 校验。
- 贡献边界规则。
- 知识节点和边约束。
- 发布门禁聚合。
- 权限判断和敏感字段过滤。
- Markdown/富文本净化。
- 版本冲突和幂等发布。

## 3. API 与数据库测试

- 公开接口不返回草稿、私有字段或内部路径。
- 管理 API 鉴权、越权和会话过期有效。
- 发布前门禁失败时禁止发布。
- 已发布内容编辑后旧版本继续公开。
- 并发编辑返回冲突而不是静默覆盖。
- 数据库可从零迁移、升级、备份和恢复。
- 事务失败不留下半发布数据。

## 4. 浏览器测试

公开路径：首页→项目列表→项目详情→架构/决策/证据→外链。

管理路径：登录→创建修订→提交审核→批准/驳回→发布→撤回/回滚。

必须验证 PC、平板、手机、键盘操作、加载、空状态、错误状态、断网和重复点击。

## 5. 旧项目验收

每个旧项目必须完成版本锁定、秘密扫描、许可证检查、干净环境运行、核心功能验证、Demo 核验、贡献边界确认和人工批准。

## 6. 发布阻断项

- 敏感信息命中。
- 未授权内容。
- 项目版本不一致。
- 权限绕过。
- 草稿泄露。
- 迁移或备份恢复失败。
- 核心浏览器路径失败。
- 构建产物与批准版本不一致。

## 7. 验收证据

保存测试报告、覆盖率、接口报告、浏览器截图、扫描报告、依赖清单、SBOM、版本 SHA、部署日志、备份恢复记录、回滚演练和人工批准记录。

## 8. Java 迁移阶段测试状态

- 当前 Java 后端单元测试和 MockMvc 接口测试可以在无数据库环境下执行，覆盖应用上下文、公开项目接口、管理员认证接口和健康接口。
- 本地 MySQL 8.0 已可用（服务 MySQL80，端口 3306，库 `personal_portfolio`）。2026-09-10 已在本库完成下列真实验证：
  - 从零建库迁移：清空全部表后由 Flyway 依次执行 V1 → V2 → V3 成功。
  - 重复启动幂等：第二次启动输出 `Successfully validated 3 migrations` 与 `Schema is up to date. No migration necessary.`。
  - 迁移完整性：V1 校验和 `1881988467`、V3 校验和 `1707037090` 与事故前记录一致，重建后的 schema 与重建前逐列逐索引一致。
  - 应用可用性：`/api/health`、`/api/public/projects`、`/api/public/learning-summaries` 返回 200；管理员登录错误口令返回 401、空口令返回 400、无令牌写入管理接口返回 401。
- 仍未完成：数据库接口读写（真实数据的创建/读取链路）、事务失败回滚、测试数据库隔离。
- 迁移事故的完整过程见 `doc/21-migration-incident-and-recovery.md`。
- 浏览器交互验收已就绪：`utils/browser-check.mjs` 用 Chrome DevTools Protocol 驱动无头 Chrome 执行真实交互（点击、翻页、按键、筛选、移动端），退出码可作为门禁。2026-09-10 在 Vue 正式前端执行 23 项，全部通过，覆盖详情抽屉图组、Escape 关闭、滚动恢复、素材类型标签、四种筛选和 390px 移动端菜单。
- 仍属于待完成证据：Tab 焦点顺序、`focus-visible` 样式、屏幕阅读器语义、断网与重复点击，以及管理端审核发布链路的浏览器路径（该链路尚未实现）。
