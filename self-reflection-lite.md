# Personal Portfolio 阶段复盘

> 只记录本项目中值得长期复用的工程经验；详细学习过程继续放在 `ai-dev-lab/REFLECTION.md`。

## 当前阶段

- 已完成：项目设计文档、V2 至 V5 高保真原型、V5 迁移到 Vue 3 正式前端、Java 17 + Spring Boot 3 后端骨架、MySQL 8 + Flyway 从零迁移验证、管理员会话、项目草稿创建与送审基础链路。
- 对外展示名与仓库名：AI Translator 对外展示为 LexiFlow，仓库与坐标仍为 `ai-translator`（ADR-013）。
- 作品集与未来实验平台保持完全隔离；作品集不执行任意代码。
- 当前阶段基线提交：`12574c4 feat: migrate V5 gallery into the Vue frontend`。

## 重要经验

| 问题/决策 | 经验 |
|---|---|
| 原型与运行页面不一致 | 原型确认后必须尽快迁移到真实前端，避免维护两套页面造成内容和视觉漂移。 |
| 作品集与实验平台 | 稳定展示域和可失败实验域必须是两个独立项目，不能共用运行时。 |
| 旧 SQLite 迁移 | 不删除旧库；使用 Alembic 的 `stamp` + `upgrade` 标准流程保留数据并补齐结构。 |
| 发布内容 | 草稿、审核版本和公开版本分离，已发布内容不能原地覆盖。 |
| 项目展示 | 合作项目必须明确团队成果和个人贡献边界，不能把团队成果包装成独立成果。 |
| 真实项目资料 | 上传前必须检查版本、授权、密钥、日志、截图、依赖、运行方式和证据。 |
| 测试策略 | 页面能打开不等于交付完成，至少要有单元测试、接口测试、构建和浏览器主流程验证。 |
| 已应用的迁移文件丢失 | 迁移一旦执行就必须与代码同批提交；文件丢失后库能跑但无法复现，属于最隐蔽的一类缺口。 |
| 用配置掩盖失败 | `ignore-migration-patterns` 之类的兜底会让工具安静通过，问题长期不可见；让工具直接失败更有价值。 |
| 校验和可复现性 | Flyway 校验和是逐行 CRC32，可以独立复算，用来判断迁移文件是否被改动、事故是否波及既有迁移。 |
| 对外名称与仓库名 | 展示名（LexiFlow）必须与仓库名（`ai-translator`）建立记录，否则审查版本和贡献时无法核对。 |
| 任务收尾 | 完成度最高的改动最容易停在"没提交"这一步；每完成一个阶段立即提交并同步状态文档。 |

## 当前待办

1. 补齐 Java 后端审核发布链路（approve、reject、publish、unpublish、审计事件）；在补齐前，Java 后端无法产出任何公开内容。
2. 修复公开知识节点接口未过滤 `published`、以及 revision number 硬编码为 1 的问题。
3. 补充数据库接口读写、事务失败回滚和测试数据库隔离验证。
4. 使用浏览器验证正式前端首页、筛选、详情抽屉、Escape 关闭、移动端菜单与后端不可用时的回退。
5. 补充三个真实项目的审查资料后，再录入公开内容（当前 `projects` 表为空，页面走前端回退数据）。
6. 完成验证后推送当前阶段版本到 Gitee。

## 复利闭环

```text
ai-dev-lab 学概念
→ ai-translator / Java 项目精读和验证
→ personal-portfolio 用自己的话整理项目、决策和证据
→ 形成可展示、可复盘、可迁移的能力
```
